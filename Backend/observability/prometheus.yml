# Prometheus scrapes the OTel Collector's prometheus exporter endpoint.
# The Collector converts OTLP metrics into Prometheus format and serves them here.
scrape_configs:
  - job_name: "otel-collector"
    scrape_interval: 15s
    static_configs:
      - targets: ["otel-collector:8889"]
